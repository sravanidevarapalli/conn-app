pipeline
{
 agent any 
 
 environment {
       environment_sandbox = 'Sandbox'
       environment_design = 'Design'
	  when{ environment name: 'environment_sandbox',value: 'Sandbox' }
	   connAppclientId=credentials('connectedAppclientid')
       connAppclientSecret=credentials('connectedAppclientsecret')
       connAppgrantType=credentials('connAppgrantType')
	   
	   }
 stages{
    stage('Build Application'){
    steps{
    bat 'mvn clean install -DskipTests'
    }
    }
    
  stage('Deploy Application in Mulesoft Cloudhub'){
   steps{
   bat 'mvn package -DskipTests deploy -DmuleDeploy -Denvironment=${environment_sandbox} -DconnectedAppClientId=${connAppclientId} -DconnectedAppClientSecret=${connAppclientSecret} -DconnectedAppGrantType=${connAppgrantType}'
   
   }
  }
}
}
